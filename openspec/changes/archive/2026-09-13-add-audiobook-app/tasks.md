## 1. 工程与数据基座

- [x] 1.1 添加 ZIPFoundation SPM 依赖（锁 tag），建立 `abm/Models/`、`abm/Services/`、`abm/Views/` 目录骨架；验证：构建通过
- [x] 1.2 实现 `BookProject` / `Chapter` / `ExportPreset` 模型与 Codable 清单；实现 `LibraryStore`（library 目录扫描、清单读写、原子保存）；验证：单元级自测（写读 round-trip）
- [x] 1.3 实现 `AppStore`（@MainActor @Observable：书籍字典、导航路由、引擎/队列状态接线）与 `AppNavigationState`；验证：构建通过

## 2. EPUB 导入

- [x] 2.1 实现 `EPUBParser`：解压（ZIPFoundation）→ container.xml → OPF 元数据（书名/作者/封面）→ 章节目录三级回退（nav.xhtml → toc.ncx → spine 推导）→ 正文剥标签清洗与字数统计；验证：用 2-3 本真实 EPUB（含无目录变体）解析出正确章节列表
- [x] 2.2 实现导入服务：文件面板（⌘N 多选）+ 拖拽接收、内容哈希去重提示、后台解析进度、入库 `library/<bookID>/`（清单 + 封面）；验证：导入后 library 目录结构完整，重复导入有提示

## 3. 应用外壳与快速单文本迁移

- [x] 3.1 实现 `MainSplitView` + `AppSidebar`（五导航项、生成中徽标、系统材质）与统一工具栏（状态灯、导入按钮 ⌘N、搜索框，⌘N 注册到 abmApp Commands）；窗口 1280×840 / 最小 1080×720；验证：导航切换正确、默认选中全部图书
- [x] 3.2 将现有 MVP 界面迁移为 `QuickTTSView`（控件与行为不变，接入 AppStore 共享引擎状态）；验证：快速合成全流程与迁移前一致，跨视图共享就绪状态

## 4. 书架

- [x] 4.1 实现 `LibraryGridView` + `BookCardView`（封面圆角阴影、书名/作者·字数、三态状态区：进度条/已就绪/待生成预估）；自适应列数；验证：与设计稿 01 对照布局一致
- [x] 4.2 接通筛选语义（全部/正在生成/已完成）与搜索过滤；生成中进度实时刷新；验证：三条筛选 + 搜索逐一手测

## 5. 图书详情

- [x] 5.1 实现 `BookDetailView` 骨架：返回/面包屑、`BookHeaderCardView`（概要 + 默认音色下拉，修改生效于未生成章节并持久化）；验证：与设计稿 02 头部一致
- [x] 5.2 实现 `ChapterQueueTable`（七列、三态徽标 + 失败态、时长列、行内试听/重新生成/删除重置）；验证：各状态行的呈现与操作正确
- [x] 5.3 实现遥测卡与 `ActiveSynthesisCard`（RTF 滚动均值、已用时/ETA、内存；章节名 + seg i/N + 波形动画 + 暂停/停止/打开音频目录）；空闲态与生成态两套呈现；验证：生成中数据实时刷新

## 6. 批量生成

- [x] 6.1 实现 `BatchQueueManager`（actor）：全书入队、暂停（段间生效）、全部停止、单章入队；未就绪先初始化；验证：一键生成→暂停→恢复→停止全流程状态正确
- [x] 6.2 章节状态机与持久化闭环（等待/生成中/已完成/失败；逐章 WAV 落盘到书籍目录、清单回写、重启恢复一致性、失败重试、删除重置清音频）；验证：中途杀进程重启后状态一致、无半成品
- [x] 6.3 与 UI 接线：一键生成按钮态切换（生成/⏸暂停）、监控条与遥测驱动、书架进度联动；验证：设计稿 02 全部元素有真实数据驱动

## 7. 播放器

- [x] 7.1 实现 `AudioPlaybackService`（AVAudioPlayer 单实例互斥封装）与 `PersistentPlayerBar`（波形 Canvas 降采样绘制、拖拽定位、±15/30s、倍速、音量、时间戳、空态）；验证：章节试听、拖拽、倍速、章末自停逐一手测
- [x] 7.2 接入章节队列试听按钮与音色中心复用；验证：新播放打断旧播放

## 8. 导出

- [x] 8.1 实现 `AudiobookExporter`：M4B（AVMutableComposition 拼接 + AVTimedMetadataGroup 章节标记 + 封面/元数据注入 + AAC 导出 .m4b）、MP3 分章（含标题/序号元数据）、WAV 复制；验证：三格式产物可播放、M4B 章节在 Apple Books/播客中可见
- [x] 8.2 实现 `ExportSheetView`（格式分段/勾选/码率/路径默认 `~/Music/Audiobooks/[书名]/` + 更改、进度、完成访达定位、无完成章节禁用导出）；验证：设计稿 03 对照 + 导出全流程

## 9. 音色中心

- [x] 9.1 实现 `VoiceLibraryView`：分类 Pills + 搜索 + 音色卡片（代码绘制头像、特质文案、试听 10s、设为默认旁白 + 默认标识）；默认旁白持久化并作用于新导入书籍；验证：与设计稿 04 对照，试听打断与默认偏好重启保持

## 10. 端到端验收

- [ ] 10.1 全流程验收：导入真实 EPUB → 书架筛选/搜索 → 详情页改默认音色 → 一键生成（暂停/恢复/单章重试）→ 章节试听 → M4B 导入 Apple Books 验证章节 → MP3/WAV 抽查 → 重启恢复状态；对照 9 份 spec 逐场景核对，问题记录进交接文档

## 11. 用户反馈变更：模型菜单 + 生成进度条（2026-09-13）

- [x] 11.1 侧边栏「音色中心」下方新增「模型」入口（状态点指示；模型面板含初始化 ⌘I + 进度条 + 音频/日志目录；菜单栏误加的模型菜单已移除）
- [x] 11.2 生成中章节行内进度条：按已完成分段/预估分段（字数÷50）估算，封顶 98%，显示估算剩余时间

## 12. 用户反馈变更：顶部栏精简 + 按钮样式（2026-09-13）

- [x] 12.1 顶部工具栏删除模型状态灯与文案（状态仅通过菜单栏「模型」菜单查看）
- [x] 12.2 「导入 EPUB」按钮改为纯文字（去加号图标）
- [x] 12.3 全局按钮样式改为小圆角矩形（RoundedRectButtonStyle 6pt 圆角，根视图统一应用；主操作按钮用强调色填充变体）

## 13. 用户反馈变更：初始化入口改为侧边栏 + 搜索框样式（2026-09-13）

- [x] 13.1 「模型」入口移到侧边栏音色中心下方（带状态点），模型面板显示状态/初始化进度/目录入口；移除菜单栏模型菜单
- [x] 13.2 工具栏搜索框改为与导入按钮同高的 6pt 圆角矩形（浅底 + 细描边）
- [x] 13.3 章节队列新增「进度」列，每行独立进度条（完成=100%绿、生成中=估算蓝+剩余时间、等待/失败=0%）
- [x] 13.4 详情页已用时/预估剩余改为中文「日时分秒」格式（高位零单位省略）
- [x] 13.5 修复崩溃：工具栏内容在 AX/工具栏托管链路下环境缺失（@Environment(AppStore.self) 强解包崩溃），toolbarContent 改为值传递（isLibrary 参数化），构建期零环境访问
- [x] 13.6 彻底修复环境崩溃：全部视图的 @Environment(AppStore/AudioPlaybackService) 注入改为构造参数显式传递（MainSplitView→各子视图→Table 单元格/Sheet），零环境查找残留
- [x] 13.7 修复"分章音频无法播放"：崩溃导致清单回写丢失、音频成孤儿（章节显等待中、无试听入口）。启动对账：磁盘有音频但状态丢失 → 恢复已完成并回填实测时长

## 14. 用户反馈变更：模型下载式初始化（2026-09-13）

- [x] 14.1 引擎切换为 HF 下载式初始化：8bit 量化版（aufklarer/CosyVoice3-0.5B-MLX-8bit；4bit 经与官方 CLI 源码核对已被下线，指南表格过时），不再使用仓库本地权重；speech_tokenizer 不在框架默认下载清单内，单独补拉
- [x] 14.2 模型面板文案更新（下载提示/缓存位置），下载进度复用现有加载进度条；spec tts-engine 初始化需求改写为下载式（注：该 spec 文件曾意外丢失，已按新方案重建并补回长文本分段/串行合成需求）
- [x] 14.3 下载进度实时化：自管预下载（HF tree API 汇总体积 → 显示「下载模型 X/总 MB（百分比）」，字节级每 MB 上报）；新增「打开模型目录」按钮（模型面板 + 快速单文本），面板显示缓存路径 ~/Library/Caches/qwen3-speech/models/
- [x] 14.4 下载/初始化拆为两步：模型面板新增「① 下载模型」（进入时离线检查缓存可用性，缓存可用显示"离线可初始化"）；「② 初始化模型」只做离线加载，缓存缺失时提示先下载（不静默联网）；生成/试听的自动路径保持透明下载
- [x] 14.5 已用时间跨章节持续累加：仅新生成会话（一键生成）清零；章节切换/暂停不重置（暂停期间不计时间），全部完成或停止后保留累计值

## 15. 用户反馈变更：移除底部播放条 + 重启丢书修复（2026-09-13）

- [x] 15.1 移除底部常驻播放条（PersistentPlayerBar.swift 删除、player 能力 delta 撤销、proposal 同步）；章节试听按钮保留（AudioPlaybackService 直接播放），音色中心试听不受影响
- [x] 15.2 修复重启丢书：清单保存用 iso8601 日期但加载用默认策略，解码必然失败被 try? 吞掉 → 书架空。修复解码策略对称 + 解码失败记日志（不再静默）；对账逻辑抽为 reconcile 纯函数

## 16. 用户反馈变更：UI 视觉与响应式优化 + 章节分段断点重连（2026-09-13）

- [x] 16.1 顶部导航与右上角按钮视觉去底色：去除详情页返回按钮及所有右上角按钮的白色圆角背景，统一采用清爽的 `.buttonStyle(.plain)` 悬停微动效果
- [x] 16.2 章节队列表格响应式撑满：移除外部写死的固定高度约束，采用 `frame(minHeight: 300, maxHeight: .infinity)` 占满窗口剩余垂直空间
- [x] 16.3 消除横向滚动条：调整各列最小/理想宽度，移除 Table 外部的额外水平内边距
- [x] 16.4 章节分段断点持久化与续传：
  - 新增 `ChapterCheckpointManager`，实现逐段 Float 采样文件（`seg_%04d.raw`）与元数据清单（`meta.json`）原子存储
  - 音色名称、文本哈希、分段数一致性校验，防脏数据或参数变更污染
  - 串行合成引擎分段循环中 0 秒命中已完成分段并跳过模型推理
  - 段间检查暂停/停止状态（`shouldStop` / `Task.isCancelled`），抛出 `SynthesisInterruptedError` 安全挂起，章节回到等待态不记失败
  - 整章合成完成合并为 PCM WAV 并自动清理临时分段；章节删除重置同步清理分段
  - 章节队列进度列展示断点状态（`已存 X/Y 段 · 可续接`，橙色进度条）

